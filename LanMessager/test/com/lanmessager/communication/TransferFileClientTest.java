package com.lanmessager.communication;

import org.junit.Test;

public class TransferFileClientTest extends BaseClientTest {

	@Test
	public void test() {
		/*TransferFileClient client = new TransferFileClient();
		try {
			client.send("localhost", CommunicationConstant.PORT_TRANSFER_FILE,
					new File("./data/send/test.txt"));
		} catch (IOException e) {
			printException(e);
		}*/
	}
}
